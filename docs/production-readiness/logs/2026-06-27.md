# Production Readiness Review: 2026-06-27

## Scope

Reviewed the local Kaizen Agents repository set:

- `kaizen-agents-org/.github`
- `kaizen-agents-org/kaizen-loop`
- `kaizen-agents-org/builder-agent`
- `kaizen-agents-org/verifier`
- `kaizen-agents-org/coderabbit`
- `kaizen-agents-org/renovate-config`

The checkout root was a collection of repositories, not a single git repository. Existing local worktree state was treated as observation only and was not reverted.

## Summary

Kaizen Agents is a strong MVP for the issue-to-ready-for-review-PR loop, but it
is not yet production-ready automation. The main blockers are verifier depth,
repeated real end-to-end evidence, safety hardening, and operational
observability.

## Verification Observed

Passed locally:

- `kaizen-loop`: `npm test`, `npm run typecheck`, `npm run build`
- `builder-agent`: `npm test`, `npm run validate:json`

Did not pass locally:

- `verifier`: `pnpm typecheck && pnpm test && pnpm schema:check` stopped
  before typecheck because pnpm reported ignored build scripts for `esbuild`.
  The local `pnpm-workspace.yaml` also had an unresolved `allowBuilds` edit, so
  the verifier development workflow was not reproducible from the checked-out
  state.

## Metrics Observed

| Metric | Value | Evidence |
| --- | --- | --- |
| Issue-to-PR success rate | unavailable | No aggregated production-readiness metric was available during this review. |
| Verification failure rate | unavailable | Mechanical verification outcomes were not aggregated across processed issues. |
| Verifier block rate | unavailable | Verifier verdict outcomes such as `block_pr` and `needs_context` were not aggregated. |
| Needs-human rate | unavailable | `kaizen:needs-human` escalations were not aggregated for the review window. |
| PR guardian failure rate | unavailable | Guardian follow-up outcomes for generated PRs were not aggregated. |
| Sandbox E2E pass count | unavailable | No real sandbox E2E evidence was found; only mocked integration coverage was observed. |
| Verifier eval agreement | unavailable | Verifier eval harness and seeded/golden corpus were documented but not implemented. |
| Verifier false-positive rate | unavailable | No eval or review-labeled false-positive data was available. |
| PR linkage success rate | unavailable | Generated PR post-creation linkage checks were not yet first-class metrics. |
| Safety-check coverage | partial | Safety docs exist, but process-tree kill, env allowlist, disk preflight, and shutdown cleanup were not all verified in implementation. |
| Open PR backlog | unavailable | Open generated PR backlog was not captured as a readiness metric in this baseline. |
| Open `kaizen` issue backlog | unavailable | Eligible `kaizen` issue backlog was not captured as a readiness metric in this baseline. |
| Repeated failure reasons | unavailable | Failure reasons were observed narratively but not counted across the review window. |
| Post-merge correction rate | unavailable | Follow-up fixes or reverts after generated PR merges were not aggregated. |
| Sync drift count | unavailable | Dogfood or shared-skill drift findings were not counted as a readiness metric in this baseline. |

## Repository / CI / PR State

- Repository state: local checkouts for the monitored repository set were
  reviewed on 2026-06-27. All repositories reported `origin/main` as the default
  branch reference, and the table below records local checkout state only.
  Ahead/behind counts compare the local `HEAD` to the named remote ref.

| Repository | Local branch | Upstream state | Default-branch alignment |
| --- | --- | --- | --- |
| `kaizen-agents-org/.github` | `codex/monthly-readiness-review` | aligned with `origin/codex/monthly-readiness-review` | 4 ahead, 7 behind `origin/main` |
| `kaizen-agents-org/kaizen-loop` | `codex/discovered-issue-routing` | aligned with `origin/codex/discovered-issue-routing` | 5 ahead, 2 behind `origin/main` |
| `kaizen-agents-org/builder-agent` | `main` | 2 behind `origin/main` | 0 ahead, 2 behind `origin/main` |
| `kaizen-agents-org/verifier` | `codex/strengthen-verifier-gate` | aligned with `origin/codex/strengthen-verifier-gate` | 4 ahead, 0 behind `origin/main` |
| `kaizen-agents-org/coderabbit` | `codex/disable-coderabbit-request-changes` | aligned with `origin/codex/disable-coderabbit-request-changes` | 0 ahead, 3 behind `origin/main` |
| `kaizen-agents-org/renovate-config` | `codex/readme-docs-overview` | aligned with `origin/codex/readme-docs-overview` | 2 ahead, 20 behind `origin/main` |

- CI state: package verification was run locally where noted above; remote CI
  status was not captured as a structured baseline.
- PR state: generated PR backlog, readiness blockers, and PR guardian outcomes
  were not captured as structured baseline metrics.
- Explicit gaps: core outcome metrics, verifier outcome rates, issue escalation
  rates, backlog counts, repeated failure counts, post-merge correction data,
  and sync drift counts were unavailable and should be collected by future
  weekly reviews rather than inferred.

## Findings

1. Verifier is still an MVP gate, not a production-quality independent reviewer.

   Evidence: `verifier/README.md` describes the current implementation as an
   MVP gate. The implemented verdict logic in
   `packages/core/src/minimal-verdict.ts` is mostly deterministic pattern
   scanning over task text, diff text, verification logs, and builder reports.
   The richer staged verifier described in `verifier/docs/SPEC.md` and
   `verifier/docs/DESIGN.md` is still roadmap work.

   Impact: The system can conservatively decide whether to open a reviewable PR, but it cannot yet claim high-confidence merge-readiness assessment.

   Needed next step: Build the first executable verifier eval harness and
   seeded-bug corpus, then expand the verifier toward intent claims, evidence
   collection, and false-positive measurement.

2. Repeated real end-to-end smoke evidence is missing.

   Evidence: `kaizen-loop` has useful mocked integration coverage for the PR
   flow, and organization docs already identify repeated end-to-end smoke tests
   as remaining hardening work. The observed tests do not prove a real GitHub
   sandbox issue can repeatedly move through builder-agent, mechanical
   verification, verifier, PR creation, issue-link recognition, and PR guardian
   follow-up.

   Impact: The highest-risk integration boundaries are still external: `gh`, actual providers, branch pushing, GitHub issue linkage, and PR follow-up.

   Needed next step: Add a controlled sandbox repository smoke test and record each run's issue, branch, PR, verification output, verifier verdict, and linkage check.

3. Safety design and implementation are not fully aligned.

   Evidence: Safety docs require process-tree termination, minimal environment
   exposure to agents, disk-space preflight, and graceful shutdown behavior.
   Current command execution kills the immediate child process, agent and shell
   commands inherit broad `process.env`, and no disk-space preflight
   implementation was observed.

   Impact: Unattended runs can leave child processes alive, expose more environment than intended, or fail late under resource pressure.

   Needed next step: Implement process-group termination, environment allowlisting, run-level timeout enforcement, disk-space checks, and shutdown cleanup tests.

4. Generated PR readiness checks are not strict enough yet.

   Evidence: `GitHubClient.createPullRequest` creates the PR and parses the URL.
   The organization MVP contract also says generated PRs should be checked for
   default-branch targeting and GitHub-recognized `closingIssuesReferences`
   before the PR is considered ready.

   Impact: A PR can appear to be created successfully while failing to close its source issue on merge, or while targeting the wrong base branch.

   Needed next step: After PR creation, verify `baseRefName`, `isDraft`, and `closingIssuesReferences` with bounded retries, mirroring the stricter sync workflow behavior.

5. Operational metrics are still too thin for sustained use.

   Evidence: Status, logs, summaries, and monitor docs exist, but
   production-readiness indicators such as PR success rate, verifier block rate,
   needs-human rate, average processing time, repeated failure reasons, and
   post-merge correction or revert rate are not yet first-class release gates.

   Impact: The system can run, but it is hard to know whether quality is improving or degrading over time.

   Needed next step: Add metrics that separate selection, builder, verification, verifier, PR creation, guardian, and human-review outcomes.

## Priority

Recommended order:

1. Make `verifier` local verification reproducible.
2. Add real sandbox E2E smoke runs and keep dated results.
3. Harden safety controls that affect unattended execution.
4. Enforce post-PR issue-link and base-branch checks.
5. Add operational metrics and use them in readiness reviews.

## Issue Candidates

These candidates are the only inputs the readiness issue-creator automation may
turn into GitHub issues. Created issue titles use the `[readiness-review]`
prefix.

| Candidate | Target repository | Status | Evidence | Documentation basis |
| --- | --- | --- | --- | --- |
| Add an executable verifier eval harness and seeded corpus | `kaizen-agents-org/verifier` | ready | The verifier is still described as an MVP gate, and no executable eval harness or seeded/golden corpus was observed. | `.github/docs/production-readiness/checklist.md`, "Verifier Quality"; `.github/docs/production-readiness/metrics.md`, "Readiness Evidence Metrics" |
| Add a controlled sandbox E2E smoke run with recorded artifacts | `kaizen-agents-org/kaizen-loop` | ready | No repeated real sandbox issue-to-PR run was found for builder-agent, mechanical verification, verifier, PR creation, issue-link recognition, and PR guardian follow-up. | `.github/docs/issue-to-pr-mvp.md`, "Product Goal"; `.github/docs/production-readiness/checklist.md`, "End-To-End Evidence" |
| Harden unattended execution safety controls | `kaizen-agents-org/kaizen-loop` | ready | Process-tree termination, environment allowlisting, disk preflight, and shutdown cleanup were identified as incomplete or unverified. | `.github/docs/architecture.md`, "Quality Gates"; `.github/docs/production-readiness/checklist.md`, "Safety And Operations" |
| Verify generated PR base branch and recognized issue linkage after creation | `kaizen-agents-org/kaizen-loop` | ready | PR creation currently parses the URL, but readiness requires checking `baseRefName`, non-draft status, and recognized `closingIssuesReferences`. | `.github/docs/issue-to-pr-mvp.md`, "Repository Contract"; `.github/docs/production-readiness/checklist.md`, "Safety And Operations" |
| Add production-readiness outcome metrics | `kaizen-agents-org/kaizen-loop` | ready | Issue-to-PR success rate, verifier block rate, needs-human rate, PR guardian failure rate, and post-merge correction rate were unavailable. | `.github/docs/production-readiness/metrics.md`, "Core Outcome Metrics"; `.github/docs/production-readiness/metrics.md`, "Operational Health Metrics" |

## Current Readiness Judgment

Ready for continued dogfooding and review-required PR generation.

Not ready for claims of production-grade autonomous maintenance, high-confidence independent verification, or unattended direct-commit operation.
